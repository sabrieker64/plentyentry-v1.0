server:
  port: 5000
spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    password: SabDab2001
    username: admin
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://plentyentry-db.czqacdk2phl7.us-east-2.rds.amazonaws.com:3306